{
  "name": "subwire-monorepo",
  "private": true,
  "license": "MIT",
  "type": "module",
  "workspaces": ["protocol", "server"],
  "scripts": {
    "test": "bun run --filter '*' test",
    "typecheck": "bun run --filter '*' typecheck",
    "build": "bun run --filter '*' build"
  }
}
